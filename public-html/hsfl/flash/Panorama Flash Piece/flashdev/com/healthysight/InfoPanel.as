package com.healthysight{	/*		The info panel is a developing tool added to the transitions panorama flash piece and 		panorama editor. It allows changes to the range of horizontal and vertical viewing motion,		as well as the amount of camera zoom. It should be disabled for the live version of the panorama		piece.		-Chris Natale	*/			import flash.events.*;	import flash.display.MovieClip;	import flash.text.TextField;		public class InfoPanel extends MovieClip{		var p:*; //stores parent clip						public function InfoPanel(_p:*){			this.x=64;			this.y=170;						p= _p;			//add event listeners			this.close_btn.addEventListener(MouseEvent.CLICK, onCloseBtnClick);			this.cameraDepth_txt.addEventListener(Event.CHANGE, onCameraDepthTxtChange);			this.xRange_txt.addEventListener(Event.CHANGE, onXRangeTxtChange);			this.yRange_txt.addEventListener(Event.CHANGE, onYRangeTxtChange);		}						private function onCloseBtnClick(e:MouseEvent){			this.stage.removeChild(this);		}				private function onCameraDepthTxtChange(e:Event){			try{				trace("p.camera.focus value: "+ p.camera.focus);				p.camera.focus = Number(this.cameraDepth_txt.text);			}			catch(err:Error){}		}				private function onXRangeTxtChange(e:Event){			p.xRange = Number(this.xRange_txt.text);			p.setXRatio();		}				private function onYRangeTxtChange(e:Event){			p.yRange = Number(this.yRange_txt.text);			p.setYRatio();		}							}}